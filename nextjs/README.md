# Next.js Starter for WebCraft Development

This starter contains a minimal Next.js app ported from the static site. Use it as a foundation to add dynamic features or an admin dashboard.

To run:
- cd nextjs
- npm install
- npm run dev

Deploy options: Vercel, Netlify (with adapter), or self-host.
